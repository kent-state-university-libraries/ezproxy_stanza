langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_ezproxy_url
    - node.type.resource
  module:
    - link
  enforced:
    module:
      - ezproxy_stanza
id: node.resource.field_ezproxy_url
field_name: field_ezproxy_url
entity_type: node
bundle: resource
label: 'EZProxy OCLC Stanza URL'
description: 'Where the original stanza from OCLC can be found (if one exists).'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  link_type: 16
  title: 0
field_type: link
